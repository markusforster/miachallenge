version: "3.1"

services:
  test:
    build:
      dockerfile: Dockerfile.test
    image: mia_test:latest

  api:
    build:
      dockerfile: Dockerfile.api
    image: mia_api:latest
    restart: always
    ports:
      - 8000:8000
